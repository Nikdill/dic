rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /users/{uid}/vocabulary/{documents=**} {
      allow read, write: if request.auth.uid == uid;
    }
  }
}
